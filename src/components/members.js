import React from "react";

const Members = props => {
  return (
    <div>
      {props..map(note => (
        <div key={.id}>
          <h2></h2>
          <p></p>
        </div>
      ))}
    </div>
  );
};

export default Members;

